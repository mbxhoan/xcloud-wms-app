# Scripts

Folder này hiện chứa command cheatsheet. Khi code Android đã được tạo, có thể thêm:

- `build-debug.sh`
- `build-release.sh`
- `install-pda.sh`
- `collect-logcat.sh`

Không commit keystore/password/secrets vào đây.
